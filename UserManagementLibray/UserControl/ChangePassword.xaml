<UserControl x:Class="UserManagementlibrary.ChangePassword"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Style/UserControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="10" Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="🔒 Change Password"
                   FontWeight="Bold"
                   Foreground="#2D6FD2"
                   Margin="0,0,0,10"
                   Grid.Row="0"
                   FontSize="24"/>

        <!-- Old Password -->
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Current Password:" FontSize="16" />
            <PasswordBox x:Name="txtOldPassword" FontSize="16"/>
        </StackPanel>

        <!-- New Password -->
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="New Password:" FontSize="16"/>
            <PasswordBox x:Name="txtNewPassword" FontSize="16"/>
        </StackPanel>

        <!-- Confirm Password -->
        <StackPanel Grid.Row="3" Orientation="Vertical">
            <TextBlock Text="Confirm Password:" FontSize="16"/>
            <PasswordBox x:Name="txtConfirmPassword" FontSize="16"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Click="BtnSave_Click" x:Name="btnSave" FontSize="16"/>
            <Button Content="Clear" Width="80" Click="BtnClear_Click" FontSize="16"/>
     
        </StackPanel>
    </Grid>
</UserControl>
